import React from 'react';

const HelloWorld = (props) => {
    return <h1>Hello, {props.greetTarget}!</h1>;
};

// je hoeft niet de volledige code uit de
// component beschikbaar te stellen
export default HelloWorld;